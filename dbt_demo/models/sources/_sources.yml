version: 2

sources:
  - name: raw
    description: Сырые выгрузки заказов и позиций — начало графа.
    schema: main
    tables:
      - name: raw_orders
        description: Заказ, его стоимость, доставка и задержка продавца.
      - name: raw_items
        description: Позиции заказа; один заказ — много позиций.
