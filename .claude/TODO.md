# TODO

Предложения агента от **2026-09-18**. Ничего из этого не согласовано и не начато —
для записи лекции проект уже готов в текущем виде. Порядок = убывание пользы.

## 1. Pre-flight прогон перед записью

- [ ] `just smoke` → убедиться, что заканчивается `RUN_SUCCESS`, затем **обязательно** `just reset`,
      чтобы вернуть стенд в состояние «до демо».

Почему важно: в этой сессии граф проверен только на загрузку определений
(`dagster definitions validate` — успешно), но ни один ассет не материализовался.
`just smoke` — единственная проверка, что демо реально поедет. Запускать его стоит
строго по чеклисту «Pre-flight за 10 минут до записи» из [`docs/RUNBOOK.md`](../docs/RUNBOOK.md),
а не мимоходом: он затирает состояние «до демо».

## 2. Убрать предупреждение Dagster, которое видно на камере

- [ ] Добавить создание пустого `.dagster_home/dagster.yaml` в рецепт `reset` в `Justfile`.

Почему: сейчас при каждом старте в консоль печатается
`WARNING - No dagster instance configuration file (dagster.yaml) found ...`.
На записи это лишний красный шум в кадре, ровно в момент запуска `just dev`.
Правка — одна строка `touch`, поведение стенда не меняется.

## 3. Минимальные тесты-сторожа

- [ ] `tests/test_defs.py`: определения загружаются, ожидаемые ключи ассетов на месте,
      ключи ингест-ассетов совпадают с источниками в `dbt_demo/models/sources/_sources.yml`.

Почему: расхождение ключей «ингест ↔ dbt sources» — самый вероятный способ незаметно
сломать DEMO 2, и ловится оно секундой теста вместо разбирательства на записи.
Тесты быстрые и не материализуют граф, так что состояние «до демо» не страдает.

## 4. Приведение к общему стандарту проектов

- [ ] `Makefile` — короткие общие команды поверх существующего `Justfile`.
- [ ] `.claude/CLAUDE.md` — инструкции уровня проекта для агентов.

Почему: этого требует общий стандарт раскладки проектов. Низкий приоритет:
на сам показ не влияет.

## 5. Мелочи

- [ ] `dagster definitions validate` помечен как superseded — в доках и скриптах перейти на `dg check defs`.
- [ ] `config/` из общего стандарта здесь, похоже, неприменим: у проекта нет ни секретов,
      ни внешних подключений — это его сознательная граница. Решить и зафиксировать,
      что каталог не нужен, чтобы не возвращаться к вопросу.
